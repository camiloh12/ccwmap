[extend]
useDefault = true

[[allowlists]]
description = "SharedPreferences flag key name 'eula_acknowledged_v1' — not a secret; it is a literal storage key used to mark that the user accepted the EULA."
regexes = [
  '''eula_acknowledged_v1''',
]
